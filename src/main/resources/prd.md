# 标题
按条件统计用户订单商品数量

# 功能描述

系统支持当前登录用户，
根据指定条件（订单号 / 状态 / 时间范围），
查询符合条件的订单，并统计其下商品总数量。

# 输入参数

订单号（可选，模糊查询）

订单状态（可选）

时间范围（可选）

# 输出结果

商品总数量（Integer）

# 业务规则

仅允许查询当前登录用户的数据

未查询到订单 → 抛出异常

订单存在但无商品 → 抛出异常

查询条件支持动态组合

全流程记录日志